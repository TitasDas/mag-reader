# Copy to `.env` and set for the hosted web/PWA build only.
# The proxy base URL that feed/article requests are appended to (encoded).
# Not needed for the Chrome extension build (it fetches directly).
# Example (Cloudflare Worker from proxy/worker.js):
VITE_FEED_PROXY="https://readstand-proxy.example.workers.dev/?url="
